{
  "name": "app-argus",
  "version": "1.0.0",
  "description": "App DocGo",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prebuild": "npm run clean",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "docgo-sdk": "github:doc9-oficial/docgo-sdk-js"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
